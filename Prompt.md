# Project Prompt — SQL Zero to Hero

## Context

This course was custom-built for me in a previous session. It was originally created from the prompt below, and the full workspace (curriculum, milestones, practice database, and exercises) already exists.

## Original prompt (that created this course)

> I want to learn SQL. I currently use Microsoft SQL Server on my local machine. Design a course to take me from zero to hero.
>
> - Feel free to search the internet for solid, ready-made resources.
> - Include an example database with example data so I can practice the SQL operations.
> - Cover everything: indexes, query execution plans, query performance, and other important database concepts.
> - Create a document I can reuse across sessions, plus a milestones file, and keep them updated as we progress.
> - Create a session-restore snapshot whenever I say **"end session"**.

## Current request (continue in this session)

I've already run the setup scripts on my local SQL Server:

1. `database/01_create_database.sql` — created the database
2. `database/02_create_tables.sql` — created the tables
3. `database/03_seed_data.sql` — seeded the data

The **TechShop** database is now live and ready.

**I want to start the course.**

Before we begin, please:

1. Review the existing workspace structure (`COURSE.md`, `MILESTONES.md`, `SESSION.md`, `database/`, `exercises/`, `notes/`).
2. Let me know if anything is unclear or if you need more context.
3. Then guide me from the current milestone in [SESSION.md](SESSION.md).
